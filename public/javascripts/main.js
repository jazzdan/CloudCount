require(["order!jquery", "order!backbone/underscore", "order!backbone/backbone", "backbone/json2", "backbone/handlebars"], function($) {
    
    $(function() {
        alert('dependencies loaded!');
    });
    
});